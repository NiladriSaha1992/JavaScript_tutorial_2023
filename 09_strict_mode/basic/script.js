"use strict";

/*
// error - 1
let hasVoterRight = false;

hasVoterRight = true;

if (hasVoterRight) {
  const voterId = prompt("Enter your voter Id: ");
  console.log("Your voter Id :", voterId);
}
*/

// error - 2
const interface = "This is wrong variable declaration";
console.log(interface);
